# Ejemplo básico de neurona artificial

entrada = [1, 2]
pesos = [0.5, 0.8]
bias = 0.2

salida = (entrada[0] * pesos[0]) + \
         (entrada[1] * pesos[1]) + bias

print("Salida de la neurona:", salida)